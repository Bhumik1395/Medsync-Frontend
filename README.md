# MedSync Frontend

Standalone React + TypeScript + Tailwind frontend for MedSync.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

## Environment

Copy `frontend/.env.example` to `.env` inside the `frontend` directory and fill in the required values.

